import { formatCurrency, generateRandomId, getTodayDate } from "./utils.js";

console.log("Currency:", formatCurrency(500));
console.log("Random ID:", generateRandomId());
console.log("Today's date:", getTodayDate());